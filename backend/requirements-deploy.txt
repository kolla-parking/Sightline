# Server-only dependency set for cloud deploys (see render.yaml).
# Excludes ultralytics (lazy-imported; only needed when a camera actually
# runs detection) and pytest — keeps the build small enough for free tiers.
fastapi==0.115.6
uvicorn[standard]==0.34.0
opencv-python-headless==4.10.0.84
numpy==2.5.2
asyncpg==0.30.0
redis==5.2.1
python-multipart==0.0.31
bcrypt==4.2.1
httpx==0.28.1
