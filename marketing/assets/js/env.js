/* Deployment config. Overwritten at deploy time by the static-site build
   (see render.yaml) to set window.SIGHTLINE_API and window.SIGHTLINE_CONSOLE.
   Intentionally empty in local dev — main.js and portal.js fall back to
   http://localhost:8000, login.html to http://localhost:5173. */
