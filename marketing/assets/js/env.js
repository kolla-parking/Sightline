/* Deployment config. Overwritten at deploy time by the static-site build
   (see render.yaml) to set window.SIGHTLINE_API for main.js and the admin
   portal. Intentionally empty in local dev — main.js and portal.js fall
   back to http://localhost:8000. */
